export type ProductDTO = {
    categoryId: string;
    name: string;
    sku: string;
    vendorId: string;
    measureUnit: string;
    packageType: string;
    price: number;
    active: boolean;
    image: string;
}